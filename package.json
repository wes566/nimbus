{
    "name": "root",
    "private": true,
    "devDependencies": {
        "lerna": "^3.15.0"
    },
    "scripts": {
        "prepare": "lerna bootstrap",
        "lint": "lerna run lint",
        "serve:demo": "lerna run serve --scope demo-www --stream",
        "clean": "lerna run clean"
    }
}
