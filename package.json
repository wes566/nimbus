{
    "name": "root",
    "private": true,
    "devDependencies": {
        "lerna": "^3.22.1"
    },
    "scripts": {
        "prepare": "lerna bootstrap",
        "build": "lerna run build",
        "clean": "lerna run clean",
        "lint": "lerna run lint",
        "bump": "lerna version --no-git-tag-version --no-push --force-publish",
        "serve:demo": "lerna run serve --scope demo-www --stream"
    }
}
